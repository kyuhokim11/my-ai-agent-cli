---
name: jobkim-project-intake
description: 기존 프로젝트에서 구현이나 리팩토링을 시작하기 전에 저장소 지침, 변경 상태, 구성, 진입점과 관련 호출 흐름을 조사해 최소 변경 계획을 세울 때 사용합니다.
---

# Project Intake

## 목표

기존 저장소의 실제 계약과 사용자 작업을 훼손하지 않고 요청 범위를 이해한다.

## 절차

1. 저장소 루트와 현재 작업 경로를 확인한다.
2. `./.codex/Codex.md`가 있으면 가장 먼저 읽는다.
3. 적용 범위의 `AGENTS.md`, `GEMINI.md`와 저장소 문서를 확인한다. 현재 사용자 요청과 실행 환경의 상위 지침을 우선하고, 저장소 지침은 작업 파일에 가까운 범위를 적용한다.
4. Git 상태에서 기존 수정과 새 작업을 구분한다.
5. 패키지 및 빌드 설정, 관련 진입점, 데이터 흐름과 검증 명령을 추적한다.
6. 확인된 사실, 불명확한 계약과 최소 변경 계획을 분리한다.
7. 사용자 요청이 변경 작업일 때만 구현으로 넘어간다.

## 제약

- 관련 없는 파일을 정리하거나 리팩토링하지 않는다.
- 기존 수정 사항을 되돌리거나 커밋 대상으로 포함하지 않는다.
- 구성, API 응답, 환경 변수 또는 실행 계약을 파일명만 보고 추측하지 않는다.
- 단순 작업에 전체 저장소 스캔을 강제하지 않는다. 실제 영향 경로를 따라 필요한 만큼만 읽는다.
