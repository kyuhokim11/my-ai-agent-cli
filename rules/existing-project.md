# Existing Project Entry Policy

이 모드는 사용자가 CLI에서 기존 프로젝트를 선택했을 때 적용된다.

코드를 제안하거나 수정하기 전에 `jobkim-project-intake` 스킬을 사용해 요청과 관련된 실제 프로젝트 맥락을 확인한다. 단순한 수정에 저장소 전체를 무차별적으로 읽지 않고, 요청과 연결된 경로부터 추적해 영향 범위가 확장될 때만 탐색을 넓힌다.

조사로 해결되지 않고 결과를 실질적으로 바꾸는 결정이 여러 개라면 가장 중요한 한 가지부터 사용자에게 확인한다.
